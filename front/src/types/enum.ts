/**
 * @file Enum.
 */
/* eslint-disable */

export enum SidebarPosition {
  Top = 'TOP',
  Bottom = 'BOTTOM',
  Dropdown = 'DROPDOWN',
}

export enum Drawer {
  Width = 275,
  MinWidth = 75,
  Tablet = 1024,
}

export enum Role {
  Student = 'STUDENT',
  Teacher = 'TEACHER',
  Admin = 'ADMIN',
  User = 'USER',
}
